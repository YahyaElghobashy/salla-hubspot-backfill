# Secrets and per-deployment config
.env
config.json
config.live.json
credentials.json
token.json

# Run state and outputs (may contain customer PII — never publish)
cursor.json
cursor.tmp
backfill.log
live.log
credit_watch.log
supervisor.out
supervisor.*.out
engine.pid
live.pid
live.lock
live_state.json
credit_watch.lock
relay_health.json
STOP
STOP.live
STOP.credits
STOP.retro
mirror/
archive/

# Python
venv/
.venv/
__pycache__/
*.pyc

# OS
.DS_Store
Thumbs.db
webui/static/brand/
